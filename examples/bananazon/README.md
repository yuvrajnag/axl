# Bananazon Example

This is a demonstration of AXL used for an e-commerce backend (Bananazon).

## Running the example

To compile the `.flow` files and generate the AXL manifest:

```bash
axl compile
```

This will read the configuration from `axl.config.json` and build the AXL app.
