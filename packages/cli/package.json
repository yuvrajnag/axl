{
  "name": "scl-axl",
  "version": "0.1.0",
  "description": "AXL command-line interface — compile, validate, doctor, init",
  "keywords": [
    "axl",
    "scl",
    "cli",
    "compiler",
    "ai",
    "mcp",
    "dsl"
  ],
  "author": "Silver Cloud Labs",
  "license": "MIT",
  "homepage": "https://axl.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/silvercloudlabs/axl.git"
  },
  "bugs": {
    "url": "https://github.com/silvercloudlabs/axl/issues"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "axl": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && npm run bundle",
   "bundle": "esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --allow-overwrite --external:prompts --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\""
  },
  "dependencies": {
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@axl/compiler": "0.1.0",
    "@axl/generators": "0.1.0",
    "@types/prompts": "^2.4.9",
    "esbuild": "^0.25.0"
  }
}
